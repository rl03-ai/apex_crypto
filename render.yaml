services:
  - type: web
    name: apex-crypto-api
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    autoDeploy: false
    plan: free
    healthCheckPath: /health
    envVars:
      # Cola aqui a connection string do Neon (modo Pooled, sem &channel_binding=require)
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "false"
      - key: SCHEDULER_ENABLED
        value: "true"
      - key: ALERT_CHECK_INTERVAL_MINUTES
        value: "10"
      - key: PORTFOLIO_UPDATE_INTERVAL_MINUTES
        value: "15"
      # Domínio do frontend — confirma o URL exacto após o primeiro deploy
      - key: ALLOWED_ORIGINS
        value: "https://apex-crypto-terminal.onrender.com"

  - type: web
    name: apex-crypto-terminal
    runtime: docker
    rootDir: frontend
    dockerfilePath: ./Dockerfile
    autoDeploy: false
    plan: free
